// An array is an ordered collection of the elements that can be accessed using a numerical index

const stringArr = ["a", "b", "c", "d", "e"];
const numArr = [1, 2, 3, 4, 5];
const boolArr = [true, false];
const mixed = ["1", 1, true, null, undefined, { name: "xyz" }, [1, 4, 8]];

console.log(mixed);
